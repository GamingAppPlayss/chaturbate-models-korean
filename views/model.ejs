<div class="flex flex-col lg:flex-row gap-8">
    <div class="w-full lg:w-3/4">
        <div class="aspect-w-16 aspect-h-9 bg-black rounded-lg overflow-hidden shadow-lg mb-6 relative" style="padding-top: 56.25%;">
            <iframe src="<%= model.chat_room_url %>" frameborder="0" allowfullscreen class="absolute top-0 left-0 w-full h-full"></iframe>
        </div>
        <h1 class="text-3xl font-bold mb-4"><%= model.username %></h1>
        <p class="text-gray-700 mb-6 text-lg leading-relaxed"><%= description %></p>
        
        <div class="bg-white p-6 rounded-lg shadow-sm mb-6 border border-gray-100">
            <h2 class="text-xl font-bold mb-4 border-b pb-2">프로필 정보</h2>
            <ul class="grid grid-cols-2 md:grid-cols-3 gap-4">
                <li class="flex flex-col"><span class="text-gray-500 text-sm">나이</span> <span class="font-medium"><%= model.age %></span></li>
                <li class="flex flex-col"><span class="text-gray-500 text-sm">위치</span> <span class="font-medium"><%= model.location || 'Unknown' %></span></li>
                <li class="flex flex-col"><span class="text-gray-500 text-sm">성별</span> <span class="font-medium"><%= model.gender %></span></li>
                <li class="flex flex-col"><span class="text-gray-500 text-sm">시청자</span> <span class="font-medium"><%= model.num_users %>명</span></li>
                <li class="flex flex-col"><span class="text-gray-500 text-sm">방 상태</span> <span class="font-medium text-green-600"><%= model.current_show === 'private' ? '프라이빗' : '공개' %></span></li>
                <li class="flex flex-col"><span class="text-gray-500 text-sm">가입일</span> <span class="font-medium"><%= new Date().toLocaleDateString() %> (Today)</span></li>
            </ul>
        </div>
    </div>
    
    <div class="w-full lg:w-1/4">
        <div class="bg-white p-6 rounded-lg shadow-lg sticky top-4 border border-gray-200">
            <a href="<%= model.chat_room_url %>" target="_blank" rel="nofollow noopener" class="btn-primary w-full text-center block text-xl py-4 mb-4 shadow-md hover:shadow-xl transition-all">
                채팅방 입장하기
            </a>
            <p class="text-sm text-gray-500 text-center mb-6">무료로 채팅에 참여하세요!</p>
            
            <h3 class="font-bold mb-3 text-lg border-t pt-4">태그</h3>
            <div class="flex flex-wrap gap-2">
                <% model.tags.forEach(tag => { %>
                    <a href="/tag/<%= makeSlug(tag) %>" class="text-xs bg-gray-100 hover:bg-gray-200 px-3 py-1.5 rounded-full text-gray-700 transition-colors">
                        #<%= tag %>
                    </a>
                <% }); %>
            </div>
        </div>
    </div>
</div>
